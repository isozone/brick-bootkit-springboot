<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.zqzqq</groupId>
        <artifactId>spring-boot3-brick-bootkit-parent</artifactId>
        <version>4.0.11</version>
    </parent>

    <artifactId>spring-boot3-brick-bootkit-adapter-modbus</artifactId>
    <name>${project.groupId}:${project.artifactId}</name>
    <description>
        Modbus RTU adapter 参考实现：基于 jSerialComm 的 Modbus RTU 传输适配器，
        演示 Brick Protocol 声明式映射如何落地为真实的设备通信。
    </description>
    <packaging>jar</packaging>

    <dependencies>
        <!-- Brick Protocol -->
        <dependency>
            <groupId>com.zqzqq</groupId>
            <artifactId>spring-boot3-brick-bootkit-protocol</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Core (DeviceRegistry, EnvelopeBus) -->
        <dependency>
            <groupId>com.zqzqq</groupId>
            <artifactId>spring-boot3-brick-bootkit-core</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- jSerialComm: platform-independent serial port access -->
        <dependency>
            <groupId>com.fazecast</groupId>
            <artifactId>jSerialComm</artifactId>
            <version>2.10.4</version>
        </dependency>

        <!-- SLF4J -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version>
                <configuration>
                    <source>17</source>
                    <target>17</target>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>